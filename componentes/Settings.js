import {View, text} from 'react-native';
import {styles} from '../assets/estilos/alistyle';

export default function Settings(){
    return(
        <View style={styles.container}>
            <Text>Estamos en configuarcion</Text>
        </View>

    );
}